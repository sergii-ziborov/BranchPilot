{
  "projectId": "b8f52d83-4699-4374-a6e5-5ea3e9fe0792",
  "shape": "package",
  "pkg": "branchpilot",
  "globalName": "BranchPilot",
  "srcDir": "src",
  "guidelinesGlob": [],
  "readmeHeader": ".design-sync/conventions.md",
  "overrides": {
    "EmptyState": { "cardMode": "column" },
    "BranchPilotLogo": { "cardMode": "column" },
    "Toaster": { "cardMode": "column" },
    "SegmentedControl": { "cardMode": "column" },
    "SelectableChipGroup": { "cardMode": "column" },
    "SeverityCountStrip": { "cardMode": "column" },
    "FindingCard": { "cardMode": "column" },
    "Meter": { "cardMode": "column" },
    "ActionCard": { "cardMode": "column" },
    "ChoiceOptionCard": { "cardMode": "column" },
    "CopyableCodeBlock": { "cardMode": "column" }
  },
  "componentSrcMap": {
    "Stat": "src/components/primitives.tsx",
    "InfoRow": "src/components/primitives.tsx",
    "PanelHeading": "src/components/PanelHeading.tsx",
    "EmptyState": "src/components/EmptyState.tsx",
    "ConflictBanner": "src/components/ConflictBanner.tsx",
    "BackToChanges": "src/components/BackToChanges.tsx",
    "ViewSwitch": "src/components/ViewSwitch.tsx",
    "StageCheckbox": "src/components/StageCheckbox.tsx",
    "BulkStageCheckbox": "src/components/StageCheckbox.tsx",
    "BranchPilotMark": "src/components/BrandIcons.tsx",
    "BranchPilotLogo": "src/components/BrandIcons.tsx",
    "LinkedinIcon": "src/components/BrandIcons.tsx",
    "ProviderRemoteCard": "src/components/ProviderRemoteCard.tsx",
    "PlannedProviderWorkflowPanel": "src/components/ProviderRemoteCard.tsx",
    "Toaster": "src/components/Toaster.tsx",
    "SegmentedControl": "src/components/SegmentedControl.tsx",
    "IconButton": "src/components/IconButton.tsx",
    "StatusPill": "src/components/StatusPill.tsx",
    "CountBadge": "src/components/CountBadge.tsx",
    "Chip": "src/components/Chip.tsx",
    "SelectableChipGroup": "src/components/SelectableChipGroup.tsx",
    "SeverityCountStrip": "src/components/SeverityCountStrip.tsx",
    "FindingCard": "src/components/FindingCard.tsx",
    "Avatar": "src/components/Avatar.tsx",
    "Meter": "src/components/Meter.tsx",
    "FileStatusToken": "src/components/FileStatusToken.tsx",
    "FileTypeIcon": "src/components/FileTypeIcon.tsx",
    "DiffStatBadges": "src/components/DiffStatBadges.tsx",
    "ActionCard": "src/components/ActionCard.tsx",
    "ChoiceOptionCard": "src/components/ChoiceOptionCard.tsx",
    "StatusDot": "src/components/StatusDot.tsx",
    "CommitRefChip": "src/components/CommitRefChip.tsx",
    "CopyableCodeBlock": "src/components/CopyableCodeBlock.tsx"
  },
  "dtsPropsFor": {
    "Stat": "  /** Label shown above the value. */\n  label: string;\n  /** The statistic value itself. */\n  value: string | number;\n  /** Visual tone of the tile. */\n  tone?: 'neutral' | 'info' | 'warn' | 'danger' | 'ok';\n  /** Optional title/tooltip text. */\n  hint?: string;",
    "InfoRow": "  /** Row label (left). */\n  label: string;\n  /** Row value (right). */\n  value: string;",
    "PanelHeading": "  /** Heading title. */\n  title: string;\n  /** Optional supporting description under the title. */\n  description?: string;\n  /** Tighter spacing variant. */\n  compact?: boolean;\n  /** Action controls rendered on the right of the header. */\n  children?: React.ReactNode;",
    "EmptyState": "  /** Whether the host bridge is ready; actions are disabled while false. */\n  apiReady: boolean;\n  /** Whether an operation is in flight. */\n  busy: boolean;\n  /** Open a local repository. */\n  chooseRepository: () => void | Promise<void>;\n  /** Controlled clone-URL field value. */\n  cloneRemoteUrl: string;\n  setCloneRemoteUrl: (value: string) => void;\n  /** Controlled clone folder-name field value. */\n  cloneTargetName: string;\n  setCloneTargetName: (value: string) => void;\n  /** Clone the entered remote. */\n  cloneRepository: () => void | Promise<void>;",
    "ConflictBanner": "  /** The in-progress operation that is mid-conflict. */\n  operation: 'merge' | 'rebase' | 'cherry-pick';\n  /** Number of files still conflicted. */\n  conflictedCount: number;\n  /** Whether an operation is in flight (disables Abort). */\n  busy: boolean;\n  /** Open the conflict-resolution flow. */\n  onResolve: () => void;\n  /** Abort the in-progress operation. */\n  onAbort: () => void | Promise<void>;",
    "BackToChanges": "  /** Navigate back to the Changes view. */\n  onClick: () => void;",
    "ViewSwitch": "  /** Active primary view. */\n  viewMode: 'dashboard' | 'changes' | 'history' | 'merge' | 'branches' | 'config' | 'stash' | 'review' | 'providers' | 'memory' | 'daily' | 'linkedin';\n  setViewMode: (mode: ViewSwitchProps['viewMode']) => void;\n  /** Badge count shown on the Changes tab; hidden when 0. */\n  changedCount: number;",
    "StageCheckbox": "  /** The file-change model this checkbox stages/unstages. */\n  change: {\n    path: string;\n    status: string;\n    staged: boolean;\n    unstaged: boolean;\n    untracked: boolean;\n    conflicted: boolean;\n  };\n  disabled: boolean;\n  /** Toggle staging for this file. */\n  onToggle: (change: StageCheckboxProps['change']) => void | Promise<void>;",
    "BulkStageCheckbox": "  /** Aggregate stage/unstage state across all changed files. */\n  state: {\n    checked: boolean;\n    mixed: boolean;\n    disabled: boolean;\n    action: 'stage_all' | 'unstage_all' | 'none';\n    label: string;\n    summary: string;\n  };\n  disabled: boolean;\n  /** Number of changed files (drives the label). */\n  changedCount: number;\n  /** Stage or unstage all files at once. */\n  onToggle: () => void | Promise<void>;",
    "BranchPilotMark": "  /** Pixel size of the square icon. */\n  size?: number;\n  className?: string;",
    "BranchPilotLogo": "  /** Pixel size of the mark; the wordmark scales relative to it. */\n  size?: number;\n  className?: string;",
    "LinkedinIcon": "  /** Pixel size; uses currentColor for fill. */\n  size?: number;\n  className?: string;",
    "ProviderRemoteCard": "  /** Provider-remote summary describing the current Git remote. */\n  remote: {\n    kind: 'github' | 'gitlab' | 'bitbucket' | 'unknown' | 'none';\n    label: string;\n    workflowLabel: string;\n    host?: string;\n    owner?: string;\n    repository?: string;\n    supported: boolean;\n    message: string;\n  };\n  /** Remote name, e.g. \"origin\". */\n  remoteName?: string;\n  /** Full remote URL, shown as a code line. */\n  remoteUrl?: string;\n  /** Whether a repository is currently open. */\n  hasRepository: boolean;",
    "PlannedProviderWorkflowPanel": "  /** Provider-remote summary for the planned-workflow notice. */\n  remote: {\n    kind: 'github' | 'gitlab' | 'bitbucket' | 'unknown' | 'none';\n    label: string;\n    workflowLabel: string;\n    host?: string;\n    owner?: string;\n    repository?: string;\n    supported: boolean;\n    message: string;\n  };",
    "Toaster": "  /** Latest transient notice; success notices auto-dismiss after ~4s. */\n  notice: string;\n  /** When true, shows a persistent busy toast. */\n  busy: boolean;\n  /** Label for the busy toast. */\n  operationLabel: string | null;\n  /** Error message; persists until dismissed. */\n  error: string | null;\n  /** Dismiss the current error toast. */\n  onDismissError: () => void;",
    "SegmentedControl": "  /** Ordered list of selectable segments rendered left-to-right as joined pill buttons. */\n  options: {\n    /** Stable value passed to onChange and compared against value for the active state. */\n    value: string\n    /** Visible text label. Omit for an icon-only segment. */\n    label?: ReactNode\n    /** Leading icon node (e.g. a lucide-react icon). Renders before the label. */\n    icon?: ReactNode\n    /** Disables this segment so it cannot be selected. */\n    disabled?: boolean\n    /** Native tooltip text for this segment. */\n    title?: string\n    /** Accessible name for this segment, useful when it is icon-only. */\n    ariaLabel?: string\n  }[]\n  /** Value of the currently active segment; the matching option gets the 'active' class. */\n  value: string\n  /** Called with the option's value when a segment is clicked. */\n  onChange: (value: string) => void\n  /** Accessible label for the whole control. */\n  ariaLabel?: string\n  /** Extra class appended after 'segmented' for a size/style variant. */\n  className?: string",
    "IconButton": "  /** Icon element rendered inside the button (e.g. a lucide-react icon). */\n  icon: ReactNode\n  /** Accessible label, used as aria-label and as the fallback tooltip. */\n  label: string\n  /** Click handler. Omit for a display-only button. */\n  onClick?: () => void\n  /** Disables the button and applies disabled styling. */\n  disabled?: boolean\n  /** Toggled/selected state; adds the 'active' class (accent border + selection bg). */\n  active?: boolean\n  /** Visual tone: 'default' or 'danger' (red hover). */\n  tone?: 'default' | 'danger'\n  /** Tooltip text; falls back to label when omitted. */\n  title?: string",
    "StatusPill": "  /** Text shown inside the pill (e.g. a working-tree state or check result). */\n  label: string\n  /** Semantic tint controlling the pill colors. Defaults to 'neutral'. */\n  tone?: 'success' | 'warn' | 'danger' | 'info' | 'neutral' | 'planned'\n  /** Optional leading icon node, typically a small lucide-react glyph (size ~14). */\n  icon?: ReactNode",
    "CountBadge": "  /** The count to display. A number, or a ReactNode for a zero-state icon fallback. */\n  count: number | ReactNode\n  /** Color tone of the pill (maps to .dash-count.tone-*). Defaults to 'default'. */\n  tone?: 'default' | 'info' | 'warn' | 'danger' | 'muted' | 'accent'",
    "Chip": "  /** Chip content (rendered inside a <strong>). */\n  label: ReactNode\n  /** Highlights the chip with the accent border/fill (adds the 'selected' class). Only meaningful with onClick. */\n  selected?: boolean\n  /** When set, the chip renders as a clickable <button>; otherwise a static <span>. */\n  onClick?: () => void\n  /** When set, renders a trailing × button (stops propagation). */\n  onRemove?: () => void\n  /** Optional leading element before the label (lucide icon, avatar, dot). */\n  leadingIcon?: ReactNode\n  /** Native title/tooltip for the chip. */\n  title?: string\n  /** Accessible label for the remove × button. Defaults to 'Remove'. */\n  removeLabel?: string",
    "SelectableChipGroup": "  /** Pill labels, in order; each becomes one selectable button. */\n  options: string[]\n  /** The selected option value; the matching pill gets the 'active' class. */\n  selected: string\n  /** Called with the clicked option's value. */\n  onSelect: (value: string) => void\n  /** Which styled container/button class set to render. Defaults to 'name-suggestions'. */\n  variant?: 'name-suggestions' | 'email-options' | 'config-email-options'\n  /** aria-label for the group container. */\n  ariaLabel?: string\n  /** Optional per-option function returning the button's title. */\n  titleFor?: (opt: string) => string\n  /** className for non-selected pills (selected always uses 'active'). Defaults to ''. */\n  inactiveClassName?: string\n  /** Disables all pills when true. */\n  disabled?: boolean",
    "SeverityCountStrip": "  /** Per-severity counts; one cell per severity. Cells always render in canonical order (critical, high, medium, low, info); any omitted severity renders as 0. */\n  counts: {\n    /** Severity bucket; drives the severity-<sev> color class. */\n    severity: 'critical' | 'high' | 'medium' | 'low' | 'info'\n    /** Number of findings in this bucket. */\n    count: number\n  }[]",
    "FindingCard": "  /** Severity level; drives the left border tint and heading tag color. */\n  severity: 'critical' | 'high' | 'medium' | 'low' | 'info'\n  /** Finding title shown bold next to the severity tag. */\n  title: string\n  /** Optional file:line reference rendered in a monospace code chip. */\n  location?: string\n  /** Main description paragraph. */\n  details: string\n  /** Optional recommendation/remediation text in a tinted block. */\n  recommendation?: string\n  /** Tighter padding/gap, smaller body text. */\n  compact?: boolean",
    "Avatar": "  /** Image URL; when omitted or it fails to load, the initials/icon fallback is shown. */\n  src?: string\n  /** Display name used for initials and the accessible label. */\n  name: string\n  /** Size variant: 'sm' (22px), 'md' (34px), 'lg' (38px). Defaults to 'md'. */\n  size?: 'sm' | 'md' | 'lg'",
    "Meter": "  /** Current progress value; clamped against max. Ignored when indeterminate. */\n  value: number\n  /** Upper bound the fill is measured against. Defaults to 100. */\n  max?: number\n  /** Minimum fill percent when value > 0, so a tiny value stays visible (e.g. 6). Defaults to 0. */\n  minPercent?: number\n  /** Render an animated indeterminate sweep instead of a determinate fill. */\n  indeterminate?: boolean\n  /** Color tone of the fill/sweep. Defaults to 'accent'. */\n  tone?: 'accent' | 'info' | 'warn' | 'danger'\n  /** Extra class appended to the track element. */\n  className?: string\n  /** Accessible label exposed via aria-label. */\n  label?: string",
    "FileStatusToken": "  /** Git file status; drives the badge color and letter token (M/A/D/R/C, ! for conflicted, ? for untracked). */\n  status: 'modified' | 'added' | 'deleted' | 'renamed' | 'copied' | 'conflicted' | 'untracked'\n  /** Optional tooltip/accessible label, e.g. \"staged / unstaged\". */\n  title?: string",
    "FileTypeIcon": "  /** File path or name; the label, tone color, and tooltip are resolved internally from the extension/filename (e.g. \"src/app.ts\" -> TS, \"package.json\" -> PK). */\n  path: string",
    "DiffStatBadges": "  /** Inserted lines, rendered in the green +N badge. */\n  additions: number\n  /** Deleted lines, rendered in the red -N badge. */\n  deletions: number\n  /** Optional files-changed count; renders a leading \"N file(s) changed\" label. */\n  filesChanged?: number\n  /** Suppress a badge whose value is 0. Default false (both always render). */\n  hideZero?: boolean\n  /** Accessible label for the group. Default 'Diff stats'. */\n  label?: string",
    "ActionCard": "  /** Leading icon node before the text (e.g. a lucide-react icon at size 18). */\n  icon: ReactNode\n  /** Bold primary label (card title). */\n  title: string\n  /** Muted supporting line beneath the title. */\n  description: string\n  /** Click handler fired when the card is activated. */\n  onClick?: () => void\n  /** Dimmed and non-interactive when true. */\n  disabled?: boolean",
    "ChoiceOptionCard": "  /** Bold heading text (rendered in a <strong>). */\n  title: string\n  /** Muted secondary text under the title. */\n  description: string\n  /** Whether this card is the chosen option; adds the 'active' class. */\n  selected: boolean\n  /** Click handler when the card is activated. Omit for a static card. */\n  onSelect?: () => void\n  /** Disables the button so it cannot be selected. */\n  disabled?: boolean",
    "StatusDot": "  /** Health/availability state controlling the dot color. */\n  state: 'ready' | 'detected' | 'limited' | 'unavailable' | 'missing'\n  /** Optional text rendered inline after the dot. */\n  label?: string\n  /** Dot family: 'model' (7px) or 'choice' (8px). Defaults to 'model'. */\n  variant?: 'model' | 'choice'",
    "CommitRefChip": "  /** Which kind of ref this chip represents; selects icon and tag/branch styling. */\n  kind: 'tag' | 'branch'\n  /** The ref text shown next to the icon (e.g. \"v1.2.0\", \"main\"). */\n  label: string\n  /** Pixel size of the lucide icon. Defaults to 12. */\n  iconSize?: number",
    "CopyableCodeBlock": "  /** Heading shown to the left of the Copy button (e.g. \"CLI command\"). */\n  title: ReactNode\n  /** Raw code/markdown/snippet text rendered verbatim inside the <pre><code> block. */\n  code: string\n  /** Click handler for the Copy button; the caller owns the clipboard write. */\n  onCopy?: () => void\n  /** Text label on the Copy button. Defaults to \"Copy\". */\n  copyLabel?: string\n  /** Wrapper variant: 'snippet' (default) for commands/config; 'preview' for taller markdown panels. */\n  variant?: 'snippet' | 'preview'\n  /** Disables the Copy button. */\n  copyDisabled?: boolean"
  }
}
